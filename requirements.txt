# Core analysis
pandas>=2.0
numpy>=1.24

# Dashboard
streamlit>=1.36

# Live market data (NSE via Yahoo Finance). Only needed for live mode.
yfinance>=0.2.40
requests>=2.31

# Tests (optional)
pytest>=8.0
